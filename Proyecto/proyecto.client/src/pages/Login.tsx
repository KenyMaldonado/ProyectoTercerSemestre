import LoginForm from '../components/Auth/LoginForm';

const Login = () => {
    return (
        <div>
        <h1>Iniciar Sesión</h1>
        <LoginForm />
        </div>
    );
};

export default Login;